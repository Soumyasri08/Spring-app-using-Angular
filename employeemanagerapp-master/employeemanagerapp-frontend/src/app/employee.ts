// We are creating an interface to specify the type of data
//that requests are going to return
export interface Employee {
  
  //these are the attributes
  
  id: number;
  name: string;
  email: string;
  jobTitle: string;
  phone: string;
  imageUrl: string;
  employeeCode: string;
}
